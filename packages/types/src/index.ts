export interface User {
  id: string
  name: string
  email: string
}

export interface Seller {
  id: string
  storeName: string
  email: string
}

export interface Product {
  id: string
  name: string
  price: number
  imageUrl: string
}