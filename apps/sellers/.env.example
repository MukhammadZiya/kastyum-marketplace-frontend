# Display name for this app (browser tab + sidebar). Root .env still supplies VITE_API_BASE_URL.
VITE_APP_NAME=Kastyum Sellers

# Main marketplace (buyers) origin — “Shopping instead?” link on seller auth pages.
# Defaults in code to http://localhost:5173 if unset.
# VITE_MARKETPLACE_ORIGIN=http://localhost:5173
